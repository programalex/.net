@model IEnumerable<Connection.consept>

@{
    ViewBag.Title = "Usuario";
}

<h2>Crud</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoDocumento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoDocumento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NumeroIdentificacion }) |
            @Html.ActionLink("Details", "Details", new { id=item.NumeroIdentificacion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NumeroIdentificacion })
        </td>
    </tr>
}

</table>
